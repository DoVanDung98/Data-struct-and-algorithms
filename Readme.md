# Data struct and Algorithms 
```
    Times complexity:
        1 < log(n) < sqrt(n) < n < nlog(n) < pow(n,2) 
        < pow(n,3) < ...< pow(2,n) < pow(3,n) < ... < pow(n,n)
```

```
Author: VanDung
```